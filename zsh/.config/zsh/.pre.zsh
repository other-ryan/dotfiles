# pre.zsh
#
# configs here are loaded before anything else. use this to override
# any local dev sources as needed. this file is ignored from git.
# examples:
#     zstyle ':rm:zsh:zephyr:prompt' use starship
#     zstyle ':rm:zsh:zephyr:prompt' theme zephyr-rm
